[project]
name = "git-backup"
version = "0.1.0"
description = "Back up a GitHub/GitLab/Codeberg user's repositories"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
]
